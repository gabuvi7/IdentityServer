@model AuthOrErrorViewModel
@{
    ViewData["Title"] = "Home Page";
}


<div class="container text-center">
    <h1>@Model.ApiResponse</h1>
</div>
@if (!(Model.Token is null))
{
    <span class="text-left">
        @Model.Token
    </span>
}

